# 간소화된 출석 흐름 검증 메모

검증 주소: `attendance.html?week=2026-08-09&club=글♥낭`

| 확인 항목 | 결과 |
| --- | --- |
| 주차·동아리 URL 파라미터 수신 | 8월 9일 시작 주차와 글♥낭 동아리 안내가 표시됨 |
| 단계명 변경 | 3단계가 `회원`이 아닌 `인원`으로 표시됨 |
| 요일 선택 | 화요일 선택 후 동아리 목록 조회 단계로 정상 이동 |

다음 단계에서 글♥낭 선택 후 직전 주차 미출석 안내와 인원 수 입력 필드를 확인한다.

글♥낭을 선택한 뒤 3단계에서 회원 이름 목록 대신 `출석 인원` 숫자 입력 필드와 `출석 제출하기` 버튼이 표시되는 것을 확인했다. 직전 주차 미출석 안내는 월간 API 조회 완료 후 같은 화면에서 추가로 확인한다.

월간 API 조회 후 글♥낭의 8월 2일 시작 주차 미출석 안내와 `지난 주차 먼저 출석하기`·`선택한 주차로 계속` 버튼이 표시됐다. `지난 주차 먼저 출석하기`를 선택하면 상단 주차 문맥이 8월 9일에서 8월 2일로 바뀌고, 스크린리더 알림도 제공되는 것을 확인했다.

375px 모바일 화면에서 선택 주차 문맥, 1~3단계 진행 표시의 `인원` 명칭, 두 열 요일 버튼의 가독성을 확인했다. 인원 입력 단계는 단일 열 입력 영역과 두 개의 직전 주차 선택 버튼으로 구성되어 있어 모바일에서도 세로 흐름으로 읽을 수 있다.

인원 수 입력 계약으로 Apps Script를 갱신한 뒤에도 브라우저에서 화요일 선택 → 동아리 목록 조회 단계가 정상적으로 진행되는 것을 확인했다. 실제 숫자 제출은 운영 출석 데이터를 변경하므로 별도 승인 없이 실행하지 않는다.

업데이트된 Apps Script 배포 이후에도 글♥낭 선택 → 출석 인원 입력 단계까지 정상 진입했다. 실제 출석 데이터 생성을 방지하기 위해 운영 브라우저에서 숫자 제출 버튼은 실행하지 않았다.

대관신청 화면의 신청서·시간표·캘린더 탭은 잘림 없이 표시되고, 시간표 탭의 고정 공간 시간표가 정상 렌더링되는 것을 확인했다. 캘린더 탭은 새 Apps Script에 `getBookings` 모드가 아직 배포되지 않아 현재 `대관 일정 데이터를 불러오지 못했습니다.` 상태를 명확하게 안내한다.

새 Apps Script URL에서 화요일 동아리 목록과 글♥낭의 8월 1~5주차 상태가 정상 조회됐다. 글♥낭 선택 뒤 8월 2일 시작 주차의 미출석을 감지해 `먼저 확인할 출석이 있습니다.` 모달과 `지난 주차 먼저 출석하기`·`선택한 주차로 계속` 선택지를 표시함을 확인했다.

375px 모바일 캡처에서 대관신청의 신청서·시간표·캘린더 탭은 동일한 너비·높이로 한 줄에 표시되고 오른쪽 경계가 잘리지 않는 것을 확인했다.
