# 배포 가이드

## 1. Google Apps Script 웹앱 배포

`google-apps-script/Code.gs`의 전체 내용을 스프레드시트에 연결된 Apps Script 프로젝트에 붙여넣습니다. 프로젝트 시간대는 `Asia/Seoul`로 지정하고, `initializeSheets`를 한 번 실행해 권한을 승인합니다. 기존 `출석부` 시트가 있으면 새 코드가 헤더를 덮어쓰지 않으므로 기존 데이터는 유지됩니다.

그다음 **배포 → 새 배포 → 웹 앱**에서 새 버전을 배포합니다. 운영 웹앱에서 필요한 사용자만 접근할 수 있도록 권한을 설정하고, 발급된 `/exec` URL을 복사합니다. `/dev` URL은 개발자 권한에 묶이므로 운영 서비스에는 사용하지 않습니다.

## 2. 웹 프로젝트 환경 변수

서비스를 실행하는 환경에 다음 값을 설정합니다. `GAS_WEB_APP_URL`은 민감 정보가 아닌 공개 웹앱 주소이지만, Apps Script를 재배포해 주소가 바뀌면 반드시 함께 변경해야 합니다.

| 변수 | 필수 | 설명 |
| --- | --- | --- |
| `GAS_WEB_APP_URL` | 예 | Google Apps Script 웹앱의 `/exec` URL입니다. |
| `NODE_ENV` | 예 | 운영 환경에는 `production`을 사용합니다. |
| `DATABASE_URL` | 선택 | 현재 출석 흐름은 필요하지 않지만, 로그인·관리 기능을 운영하면 필요합니다. |

## 3. GitHub 검증 자동화

저장소의 `.github/workflows/verify.yml`은 `main` 푸시와 Pull Request에서 `pnpm test`, `pnpm check`, `pnpm build`를 실행합니다. Google Apps Script 호출은 단위 테스트에서 모의 응답 정규화로 검증하므로 GitHub Actions에 웹앱 URL을 넣을 필요가 없습니다.

## 4. 운영 점검

배포 후 아래 URL을 확인합니다. `health` 응답에 `ok: true`가 표시되면 Apps Script 기본 연결이 정상입니다.

```text
https://script.google.com/macros/s/배포_ID/exec?mode=health
```

월간 출석 응답은 아래 형식입니다.

```text
https://script.google.com/macros/s/배포_ID/exec?mode=getMonthlyAttendanceStatus&year=2026&month=8
```

응답의 각 동아리는 `weeks` 배열 안에 `index`, `start`, `end`, `completed`를 가집니다. 웹 화면은 이 응답을 기준으로 완료 동아리를 숨기고, 미완료 주차만 출석 화면으로 연결합니다.

## 5. 간소화된 출석 제출 적용

최신 `Code.gs`는 회원 이름 대신 **출석 인원 수**를 받습니다. 웹 화면은 `attendanceCount`에 1 이상의 정수를 보내며, 출석부의 `출석인원` 열에 숫자를 기록합니다. 이 변경을 적용하려면 `google-apps-script/Code.gs` 전체를 다시 반영하고 웹앱을 새 버전으로 배포해야 합니다.

## 6. 대관·외부활동 조회 모드 배포 확인

2026-08-14 재배포 후 운영 `/exec` URL을 확인한 결과, `getBookings`와 `getActivities`는 모두 빈 배열 `[]`을 반환했습니다. 이는 두 조회 모드가 운영 웹앱에 정상 배포됐고, 아직 대관·외부활동 등록 데이터가 없음을 의미합니다.

같은 시점의 `health` 응답은 `{"ok":true,"version":"2026-08-14","timeZone":"Asia/Seoul"}`로 확인됐습니다. 따라서 현재 운영 웹앱은 재작성된 배포 코드 버전과 `Asia/Seoul` 시간대를 사용하고 있습니다.

다음 네 가지 모드를 포함한 `Code.gs` 전체를 반영한 뒤 **배포 → 배포 관리 → 수정 → 새 버전**으로 웹앱 버전을 갱신해야 합니다.

| 모드 | 용도 | 배포 후 확인 URL |
| --- | --- | --- |
| `getBookings` | 대관 캘린더 조회 | `/exec?mode=getBookings` |
| `getActivities` | 외부활동 캘린더 조회 | `/exec?mode=getActivities` |
| `submitBooking` | 대관 신청 저장 | 운영 테스트 데이터로만 확인 |
| `submitExternal` | 외부활동 등록 | 운영 테스트 데이터로만 확인 |

위 조회 URL이 빈 배열 `[]` 또는 데이터 배열을 반환하면 화면의 오류 안내가 정상적인 빈 상태 또는 일정 목록으로 전환됩니다.

## 7. 운영 연동 확인 기록

사용자 승인에 따라 2026-08-14에 외부활동 시트에 아래 연동 확인용 테스트 1건을 등록했습니다. 동일한 요청 ID로 한 번 더 요청한 뒤에도 `getActivities` 결과에는 해당 행이 한 건만 남아, 요청 ID 기반 중복 저장 방지가 동작함을 확인했습니다.

| 항목 | 기록 값 |
| --- | --- |
| 동아리명 | 임태빈 |
| 행사명 | 연동 확인용 테스트 |
| 행사 일시 | 2026-08-14 17:06 (Asia/Seoul) |
| 확인 결과 | 외부활동 일정 달력과 상세 모달에 정상 표시 |

이 행은 실제 운영 안내가 아닌 테스트 기록이므로, 확인이 끝난 뒤 스프레드시트의 **외부활동** 시트에서 해당 한 행을 직접 삭제하면 됩니다.

## 8. 공개 배포 반영 확인

2026-08-14 최신 체크포인트 `8e15d422` 직후 개발 미리보기에서는 공간 이용 예약의 남양주문화재단 외부 연결, 하단 대관 확인 버튼, `?tab=calendar` 대관 캘린더 직접 진입이 모두 확인됐습니다. 같은 시점의 공개 도메인은 이전 화면을 반환해, 자동 배포 반영 상태를 추가로 확인해야 합니다.

## 9. 프로그램 홍보 Google Sheet 운영

프로그램 홍보 화면은 Google Apps Script의 `getPrograms`를 먼저 읽고, 해당 모드가 아직 배포되지 않았거나 응답하지 않으면 웹 데이터베이스의 공개 프로그램을 대체로 읽습니다. 운영 시작 시에는 최신 `google-apps-script/Code.gs`를 전체 반영하고 `initializeSheets`를 한 번 실행한 뒤, **배포 관리 → 수정 → 새 버전**으로 웹 앱을 재배포합니다. 이 과정은 기존 출석·대관·외부활동 시트의 헤더와 데이터를 덮어쓰지 않으며, `프로그램` 시트와 필요한 열만 새로 준비합니다.

배포가 끝나면 아래 주소가 빈 배열 `[]` 또는 공개 프로그램 배열을 반환하는지 확인합니다. 빈 배열은 오류가 아니라 아직 공개된 프로그램이 없다는 뜻입니다.

```text
https://script.google.com/macros/s/배포_ID/exec?mode=getPrograms
```

`프로그램` 시트에서는 **한 행이 하나의 프로그램**입니다. 필수 항목은 `프로그램ID`, `제목`, `한 줄 소개`, `공개 여부`이며, 공개 여부가 `공개`, `예`, `true`, `1` 중 하나인 행만 사이트에 표시됩니다. 프로그램ID는 수정해도 변하지 않는 영문·숫자·하이픈 조합을 권장합니다.

| 열 | 입력 방식 | 공개 화면에서의 용도 |
| --- | --- | --- |
| 프로그램ID | 필수, 고유값 | 상세 페이지 URL과 ICS 일정 파일 식별자 |
| 제목·한 줄 소개·상세 소개 | 필수/권장 | 목록 카드와 상세 소개 |
| 주제·대상·장소 | 권장 | 목록 필터와 기본 정보 |
| 시작 일시·종료 일시 | `YYYY-MM-DD HH:MM` 또는 시트 날짜/시간 | 프로그램 캘린더·일정 저장 파일 |
| 모집 마감일·모집 상태 | 권장 | 모집 중·마감 임박·마감 상태 표시 |
| 신청 링크·신청처 | 권장 | 공식 신청 바로가기 버튼 |
| 문의처·신청 전 확인 | 권장 | 상세 안내와 신청 전 확인 목록 |
| 사진 URL | 선택 | 카드·상세 대표 사진 |
| 공개 여부 | 필수 | 공개 행만 `getPrograms`로 반환 |

`모집 상태`에는 `예정`, `모집 중`, `마감 임박`, `마감` 또는 `upcoming`, `open`, `closing-soon`, `closed`를 사용할 수 있습니다. 웹은 **마감일이 지나면 자동으로 모집 마감**, 마감까지 72시간 이내면 **마감 임박**으로 보여 줍니다. 따라서 실제 모집을 중단할 때에는 신청처에서도 접수를 종료하고, 시트의 모집 상태를 `마감`으로 바꾸거나 마감일을 정확히 입력해야 합니다.

### 관리자 화면에서 프로그램 1건 등록하기

관리자 입력 화면을 사용하는 경우에는 Google Sheet를 직접 편집하거나 명령어로 사진을 올릴 필요가 없습니다. 메인 오른쪽 상단 **관리자**를 누르고, 문화의집 소유자 계정으로 로그인한 다음 다음 순서로 입력합니다.

1. 왼쪽 상단의 `+` 버튼을 누르고 `프로그램 ID`, `프로그램 제목`, `한 줄 소개`를 입력합니다. 프로그램 ID는 처음 저장한 후에는 바꾸지 않습니다.
2. **사진 선택**을 눌러 JPG·PNG·WEBP 대표 사진을 고릅니다. 5MB 이하의 공개 동의된 사진만 올릴 수 있으며, 업로드가 끝나면 미리보기가 나타납니다. 별도로 주소를 복사하거나 사진 URL을 입력할 필요가 없습니다.
3. 장소·대상·일정·모집 마감일과 상세 소개를 채웁니다. `신청 전 확인`에는 한 줄에 한 항목씩 입력합니다.
4. 남양주문화재단 접수는 `신청처: 남양주문화재단`, 네이버폼 접수는 `신청처: 네이버폼`을 선택한 뒤 공식 `https://` 링크를 붙여넣습니다. 아직 링크가 없으면 `신청 링크 준비 중`을 선택합니다.
5. 처음에는 `비공개(작성 중)`으로 저장해 내용을 확인합니다. 공개할 준비가 끝나면 `공개`로 바꾸고 다시 저장하면 `/programs`에 반영됩니다.

관리자 화면에서 등록한 내용은 웹 데이터베이스에 저장됩니다. 같은 프로그램 ID가 Google Sheet에 이미 있을 때에는 공식 Sheet 행이 공개 화면에 우선 표시되므로, 한 프로그램을 두 곳에 중복 등록하지 않습니다.

### Google Sheet로 프로그램 1건 등록하기

1. 최신 `Code.gs`를 반영·재배포한 뒤 `프로그램` 시트에 새 행을 추가합니다.
2. `프로그램ID`, `제목`, `한 줄 소개`, `공개 여부`를 먼저 입력합니다. 예시는 `2026-autumn-ceramic`, `가을 도자기 교실`, `흙으로 만드는 나만의 생활 소품`, `공개`입니다.
3. 시작·종료 일시, 모집 마감일, 장소, 대상, 상세 소개를 채웁니다. 일시는 `YYYY-MM-DD HH:MM` 형식을 사용합니다.
4. 공식 신청처가 준비됐으면 `신청처`에 `nyjcf` 또는 `naver`, `신청 링크`에 전체 `https://` 주소를 입력합니다. 준비 전이라면 둘 다 비워 둡니다.
5. 대표 사진이 있으면 아래 사진 업로드 절차로 받은 `/manus-storage/...` 주소를 `사진 URL`에 붙여넣습니다. 마지막으로 `getPrograms` 주소 또는 `/programs` 화면에서 공개 상태를 확인합니다.

### 공식 신청 링크 입력 원칙

공식 신청은 문화의집 웹사이트에서 별도로 받지 않고, 프로그램별로 확인된 공식 접수처로만 연결합니다. 남양주문화재단 페이지라면 `신청처`에 `nyjcf`, 네이버폼이면 `naver`를 입력하고 `신청 링크`에는 전체 `https://` 주소를 넣습니다. 다른 공식 접수처는 `other`를 입력할 수 있습니다. 신청 링크가 준비되지 않았거나 모집이 마감된 경우에는 바로가기 버튼 대신 안내 문구가 표시됩니다.

| 운영 상황 | 신청처 | 신청 링크 | 화면 동작 |
| --- | --- | --- | --- |
| 남양주문화재단 접수 | `nyjcf` | 해당 프로그램의 공식 상세·예약 URL | 새 창에서 공식 신청처 열기 |
| 네이버폼 접수 | `naver` | 담당자가 검수한 네이버폼 URL | 새 창에서 공식 신청처 열기 |
| 접수처 준비 중 | `none` 또는 빈 값 | 빈 값 | “공식 신청 링크 준비 중” 표시 |
| 모집 마감 | 어떤 값이든 가능 | 보관 가능 | 신청 버튼 대신 마감·문의 안내 표시 |

### 프로그램 사진 업로드 절차

사진 파일 자체를 Google Sheet나 데이터베이스에 넣지 않습니다. 공개 동의를 확인한 사진만 아래 순서로 S3 저장소에 올리고, 반환된 주소만 `사진 URL`에 입력합니다.

1. 원본 사진을 프로젝트 밖의 `/home/ubuntu/webdev-static-assets/`에 보관합니다. 이 폴더에는 예를 들어 `2026-autumn-class-poster.jpg`처럼 프로그램과 시기를 알 수 있는 이름을 사용합니다.
2. 프로젝트 작업 환경에서 `manus-upload-file --webdev /home/ubuntu/webdev-static-assets/2026-autumn-class-poster.jpg`를 실행합니다.
3. 명령이 반환한 `/manus-storage/...` 경로를 그대로 복사합니다. 경로를 다른 도메인 주소로 바꾸거나 프로젝트의 `client/public`에 사진을 넣지 않습니다.
4. 복사한 경로를 `프로그램` 시트의 `사진 URL` 열에 붙여넣고 공개 여부를 확인합니다. 웹은 이 S3 경로를 카드와 상세 대표 이미지로 표시합니다.

> 프로그램 사진에는 식별 가능한 참여자의 공개 동의와 초상권·저작권 확인이 선행되어야 합니다. 동의가 확인되지 않은 사진은 업로드하지 말고 사진 URL을 비워 둡니다.

## 10. 2026-08-15 출석·대관 연동 확인 테스트 기록

사용자 승인에 따라 실제 등록 경로와 중복 방지를 확인하기 위해 아래 두 건을 등록했습니다. 두 행은 운영 기록이 아니므로 확인 후 각 시트에서 직접 삭제하면 됩니다. 출석 테스트는 7월 월간 상태에서 미제출이던 주차가 완료로 바뀌는 것까지 확인했고, 대관 테스트는 `getBookings` 결과와 대관 캘린더의 8월 19일 오후 2시 이벤트 표시까지 확인했습니다.

| 구분 | 시트 | 테스트 내용 | 요청 ID | 중복 확인 | 삭제 방법 |
| --- | --- | --- | --- | --- | --- |
| 출석 | 출석부 | 궁채와열채사이, 2026-07-01, 출석 인원 1명 | `test-attendance-gungchae-20260701-1786754799` | 같은 주차 재전송 시 `alreadySubmitted: true` | `출석부`에서 동아리명과 날짜가 일치하는 한 행 삭제 |
| 대관 | 대관신청 | 임태빈 (연동 확인용 테스트), 2026-08-19 14:00, 1시간, 1명 | `test-booking-20260819-1786754845` | 같은 요청 ID 재전송 시 `alreadySubmitted: true` | `대관신청`에서 요청 ID가 일치하는 한 행 삭제 |

출석 테스트에서 앞서 시도한 `개나리라인댄스`의 8월 12일과 7월 29일은 이미 해당 주차 출석이 존재해 저장되지 않았습니다. 따라서 별도 삭제할 행이 없습니다.
