# 동아리별 월간 출석 확인 UX 제안

## 권장안: 미출석 우선 동아리 목록 + 주차 드릴다운

이번 달 출석 확인을 누르면 먼저 **동아리별 목록**을 보여준다. 각 행에는 동아리명, 이번 달 완료 주차 수, 미출석 주차 수, 가장 가까운 미출석 주차를 표시한다. 미출석이 있는 동아리를 위로 정렬하고, 완료된 동아리는 아래에 접어 둔다.

사용자가 동아리 행을 누르면 이번 달 1~5주차를 가로 칸으로 표시한다. 완료 주차는 체크, 미출석 주차는 테두리 버튼으로 보이게 하고, 미출석 칸을 누르면 해당 주차가 전달된 출석 화면으로 이동한다. 즉, 한 번에 전체 현황을 보고, 두 번의 탭 안에 필요한 출석부를 열 수 있다.

## 보조 기능

- 상단에 `미출석만 보기` 토글과 동아리명 검색을 둔다.
- 요약에는 `이번 달 미출석 동아리 12개`처럼 실제 데이터 기반 집계만 표시한다.
- 동아리 행에는 `마지막 제출: 8월 8일`을 보조 정보로 제공해 담당자가 최근 처리를 빠르게 판단하도록 돕는다.
- 모바일에서는 주차 칸을 5개 고정 폭 버튼으로 유지하고, 동아리 카드는 아코디언으로 열어 스크롤 비용을 줄인다.

## 구현 순서

1. 원본 API가 동아리별·주차별 제출 이력을 제공하는지 확인한다.
2. 이력이 충분하면 서버에서 월간 상태를 정규화해 동아리 배열로 반환한다.
3. 미출석 우선 목록과 주차 드릴다운을 구현한다.
4. 이력이 없으면 현재의 전체 미출석 카드와 동아리 필터부터 제공하고, 백엔드 데이터 구조 확장 후 주차별 드릴다운을 추가한다.
